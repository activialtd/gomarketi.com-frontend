NEXT_PUBLIC_API_URL=https://api.gomarketi.com
# Domain only — no protocol prefix, no trailing =
NEXT_PUBLIC_STORE_DOMAIN=gomarketi.com
# Set this in Vercel environment variables too (it cannot live in a committed file
# if you treat client IDs as semi-sensitive, but it is safe to commit since it's
# a public identifier that appears in every page's HTML anyway)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=649332589063-trnpab4du1t75756u5lkil77d67c92la.apps.googleusercontent.com